# ===================================================================
# 📁 ملف إعدادات مشروع Flutter - "Fb Explorer Fyras"
# ===================================================================
# 
# 🎯 التطبيق: Fb Explorer Fyras
# 📅 التاريخ: 16 ديسمبر 2025
# 🔧 الإصدار: 1.0.0 (المُحسّن - مع تصحيحات كاملة)
# ===================================================================

# اسم التطبيق كما سيظهر في متجر التطبيقات
name: fb_explorer_fyras

# وصف التطبيق
description: تطبيق لاستكشاف وجمع إعلانات فيسبوك بشكل ذكي ومنظم

# إصدار التطبيق (الإصدار الأول المُحسّن)
version: 1.0.0+1

# إعدادات بيئة دارت
environment:
  sdk: ">=3.0.0 <4.0.0"

# ===================================================================
# 📦 التبعيات الخارجية
# ===================================================================
dependencies:
  flutter:
    sdk: flutter
  
  # 📱 لعرض صفحات الويب
  webview_flutter: ^4.8.0
  
  # 💾 للوصول إلى مجلدات التخزين
  path_provider: ^2.1.3
  
  # 🔐 للتخزين الآمن للبيانات الحساسة
  flutter_secure_storage: ^9.1.0
  
  # 🌐 للتحقق من اتصال الإنترنت (إضافة جديدة)
  connectivity_plus: ^5.0.0

# ===================================================================
# ⚙️ إعدادات Flutter
# ===================================================================
flutter:
  uses-material-design: true
  
  # تعريف ملفات الأصول
  assets:
    - facebook_scraper.js      # كود تحليل فيسبوك
    - ai_prompt.txt            # الموجه النصي (النسخة الافتراضية)

# ===================================================================
# 🤖 إعدادات أندرويد
# ===================================================================
android:
  usesCleartextTraffic: true  # للتوافق مع جميع روابط فيسبوك
